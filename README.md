# Youtube-Searching
This uses microphone input as the argument for searching. The library used is speech_recognition. We can also use the pyttsx3 library but it involves extra steps of 
converting the speech to text, where as here the function 'recognize_google' is used to directly make sure that speech is getting converted into text 
